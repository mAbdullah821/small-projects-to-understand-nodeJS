const rootPath = '..';
const startFromOrder = 25;
const increaseBy = 1; // can be [positive (or) 0 (or) negative]
const paddingLength = 2;

module.exports = {
  rootPath,
  startFromOrder,
  increaseBy,
  paddingLength,
};
